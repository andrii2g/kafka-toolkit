# Samples

The Docker Compose demo uses the producer and consumer projects in this
directory. `topics.sh` can be run manually to create or repair the demo topics.

```bash
./samples/topics.sh --bootstrap localhost:9092
```
